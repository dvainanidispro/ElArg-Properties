<div class="container-fluid">
    <div class="row">
        <!-- Breadcrumb -->
        <div class="col-12 mb-3">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/properties/properties">Ακίνητα</a></li>
                    <li class="breadcrumb-item active" aria-current="page">
                        {{#if propertyDetails}}
                            {{join propertyDetails.address propertyDetails.appartment_number}}
                        {{else}}
                            Νέο Ακίνητο
                        {{/if}}
                    </li>
                </ol>
            </nav>
        </div>
        
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">
                        <i class="{{or propertyDetails 'ti-pencil' 'ti-plus'}} me-2"></i>
                        {{#if propertyDetails}}
                            {{join propertyDetails.address propertyDetails.appartment_number}}
                        {{else}}
                            Νέο Ακίνητο
                        {{/if}}
                    </h4>
                </div>
                <div class="card-body">
                    <form id="propertyForm">
                        <div class="row">
                            {{#if propertyDetails}}
                            <div class="d-none">
                                <label for="propertyId" class="form-label fw-bold">ID Ακινήτου</label>
                                <input type="text" class="form-control" id="propertyId" name="id" value="{{propertyDetails.id}}" readonly>
                                <div class="form-text text-muted">Δεν επιτρέπεται επεξεργασία του ID</div>
                            </div>
                            {{/if}}

                            <div class="col-md-6 mb-3">
                              <label for="propertyAddress" class="form-label fw-bold">Διεύθυνση *</label>
                              <input type="text" class="form-control" id="propertyAddress" name="address" value="{{or propertyDetails.address '' ''}}" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="propertyAppartmentNumber" class="form-label fw-bold">Αριθμός Διαμερίσματος</label>
                                <input type="text" class="form-control" id="propertyAppartmentNumber" name="appartment_number" value="{{or propertyDetails.appartment_number '' ''}}">
                                <div class="form-text text-muted">Αριθμός διαμερίσματος εντός κτιρίου (αν διατίθεται)</div>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label for="propertyDepartment" class="form-label fw-bold">Δημοτικό Διαμέρισμα</label>
                                <input type="text" class="form-control" id="propertyDepartment" name="department" value="{{or propertyDetails.department '' ''}}">
                            </div>
                            <div class="col-md-12 mb-3">
                                <label for="isPartOfOther" class="form-label fw-bold">Αποτελεί τμήμα άλλου ακινήτου του Δήμου</label>
                                <select class="form-select" id="isPartOfOther" name="is_part_of_other">
                                    <option value="false" {{#unless propertyDetails.is_part_of_other}}selected{{/unless}}>Όχι</option>
                                    <option value="true" {{#if propertyDetails.is_part_of_other}}selected{{/if}}>Ναι</option>
                                </select>
                                <div class="form-text text-muted">Το ακίνητο είναι τμήμα άλλου ακινήτου (π.χ. διαμέρισμα, κατάστημα) το οποίο ανήκει στο Δήμο</div>
                            </div>

                            <hr>
                            
                            <div class="col-12 mb-3">
                                <label for="propertyType" class="form-label fw-bold">Τύπος Ακινήτου</label>
                                <select class="form-select" id="propertyType" name="asset_type">
                                    <option value="owned" {{#if (eq propertyDetails.asset_type 'owned')}}selected{{/if}}>{{assetTypeText 'owned'}}</option>
                                    <option value="leased_out" {{#if (eq propertyDetails.asset_type 'leased_out')}}selected{{/if}}>{{assetTypeText 'leased_out'}}</option>
                                    <option value="rented" {{#if (eq propertyDetails.asset_type 'rented')}}selected{{/if}}>{{assetTypeText 'rented'}}</option>
                                    <option value="granted" {{#if (eq propertyDetails.asset_type 'granted')}}selected{{/if}}>{{assetTypeText 'granted'}}</option>
                                </select>
                            </div>
                            <div class="col-12 mb-3">
                                <label class="form-label fw-bold">Μίσθωση</label>
                                <div class="input-group">
                                    <input type="text" class="form-control" 
                                           value="{{#if propertyDetails.lease}}[{{leaseDirectionShortText propertyDetails.lease.lease_direction}}] {{propertyDetails.lease.party.name}} - Λήξη: {{date propertyDetails.lease.lease_end}}{{else}}Χωρίς ενεργή μίσθωση{{/if}}" 
                                           readonly>
                                    {{#if propertyDetails}}
                                        <div class="btn-group">
                                            {{#if propertyDetails.lease}}
                                                <a href="/properties/leases/{{propertyDetails.lease.id}}" class="btn btn-primary" type="button">Προβολή<br>Μίσθωσης</a>
                                            {{/if}}
                                            <a href="/properties/properties/{{propertyDetails.id}}/leases/history" class="btn btn-secondary" type="button">Ιστορικό<br>Μισθώσεων</a>
                                            {{#if (can 'edit:content')}}
                                                <a href="/properties/leases/new?property_id={{propertyDetails.id}}&redirect=/properties/properties/{{propertyDetails.id}}" class="btn btn-success" type="button">Δημιουργία<br>Μίσθωσης</a>
                                            {{/if}}
                                        </div>
                                    {{/if}}
                                </div>
                                {{#unless propertyDetails}}
                                <div class="form-text text-muted">Θα πρέπει να αποθηκεύσετε το νέο ακίνητο πριν μπορέσετε να προσθέσετε πληροφορίες μίσθωσης</div>
                                {{/unless}}
                            </div>

                            <hr>
                            
                            <div class="col-md-6 mb-3">
                                <label for="constructionYear" class="form-label fw-bold">Έτος Κατασκευής</label>
                                <input type="number" class="form-control" id="constructionYear" name="construction_year" value="{{or propertyDetails.construction_year '' ''}}">
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="propertyKaek" class="form-label fw-bold">ΚΑΕΚ</label>
                                <input type="text" class="form-control" id="propertyKaek" name="kaek" value="{{or propertyDetails.kaek '' ''}}">
                                <div class="form-text text-muted">Κωδικός Αναγνώρισης Ειδικής Κατασκευής</div>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="propertyArea" class="form-label fw-bold">Επιφάνεια (τ.μ.)</label>
                                <input type="number" class="form-control" id="propertyArea" name="area" value="{{or propertyDetails.area '' ''}}">
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="propertyUsage" class="form-label fw-bold">Χρήση Ακινήτου</label>
                                <input type="text" class="form-control" id="propertyUsage" name="usage" value="{{or propertyDetails.usage '' ''}}">
                            </div>
                            
                            
                            
                            <div class="col-md-12 mb-3">
                                <label for="propertyDescription" class="form-label fw-bold">Περιγραφή - Σημειώσεις</label>
                                <textarea class="form-control" id="propertyDescription" name="description" rows="3">{{or propertyDetails.description '' ''}}</textarea>
                            </div>
                            
                            <hr>
                            <div class="col-12 mb-3">
                                <label for="ownershipStatus" class="form-label fw-bold">Ιδιοκτησιακό Καθεστώς</label>
                                <select class="form-select" id="ownershipStatus" name="ownership_status">
                                    <option value="">Επιλέξτε...</option>
                                    <option value="not_owned" {{#if (eq propertyDetails.ownership_status 'not_owned')}}selected{{/if}}>{{ownershipStatusText 'not_owned'}}</option>
                                    <option value="sole_ownership" {{#if (eq propertyDetails.ownership_status 'sole_ownership')}}selected{{/if}}>{{ownershipStatusText 'sole_ownership'}}</option>
                                    <option value="shared_ownership" {{#if (eq propertyDetails.ownership_status 'shared_ownership')}}selected{{/if}}>{{ownershipStatusText 'shared_ownership'}}</option>
                                </select>
                            </div>
                            
                            <div class="col-md-12 mb-3">
                                <label for="ownershipDetails" class="form-label fw-bold">Λεπτομέρειες Ιδιοκτησίας</label>
                                <textarea class="form-control" id="ownershipDetails" name="ownership_details" rows="2">{{or propertyDetails.ownership_details '' ''}}</textarea>
                                <div class="form-text text-muted">Περιγραφή ιδιοκτησιακού καθεστώτος, ποσοστά συνιδιοκτησίας κλπ.</div>
                            </div>
                            
                            <hr>
                                                       
                            <div class="col-12 mb-3">
                                <label for="fileServerLink" class="form-label fw-bold">Link Αρχείων</label>
                                <div class="input-group">
                                    <input type="text" class="form-control" id="fileServerLink" name="file_server_link" value="{{or propertyDetails.file_server_link '' ''}}">
                                    <button class="btn btn-secondary btn-icon" type="button" id="copyFileLinkBtn" title="Αντιγραφή διαδρομής">
                                        <i class="ti-clipboard me-1"></i>
                                        <span>Αντιγραφή</span>
                                    </button>
                                </div>
                                <div class="form-text text-muted">Διαδρομή αρχείων στον file server. Κάντε επικόλληση στον Windows Explorer.</div>
                            </div>
                            
                            <div class="col-md-12 mb-3">
                                <label for="propertyActive" class="form-label fw-bold">Κατάσταση</label>
                                <select class="form-select" id="propertyActive" name="active">
                                    <option value="true" {{#unless (eq propertyDetails.active false)}}selected{{/unless}}>Ενεργό</option>
                                    <option value="false" {{#if (eq propertyDetails.active false)}}selected{{/if}}>Ανενεργό</option>
                                </select>
                            </div>
                        </div>
                        
                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="d-flex gap-2">
                                    {{#if (can 'edit:content')}}
                                    <button type="submit" class="btn btn-success btn-icon">
                                        <bootstrap-icon icon="check-circle-fill"></bootstrap-icon>
                                        <div>{{check propertyDetails "Αποθήκευση" "Δημιουργία"}}</div>
                                    </button>
                                    {{/if}}
                                    <button type="button" class="btn btn-secondary btn-icon" onclick="history.back()">
                                        <bootstrap-icon icon="x-circle-fill"></bootstrap-icon>
                                        <div>Ακύρωση</div>
                                    </button>
                                    <bootstrap-spinner class="d-none"></bootstrap-spinner>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="ti-settings me-2"></i>
                        Άλλες Ενέργειες
                    </h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-4">
                        {{#if propertyDetails}}
                        <button type="button" class="btn btn-danger delete-property-btn {{#unless (can 'edit:content')}}d-none{{/unless}}"
                                data-property-id="{{propertyDetails.id}}" 
                                data-property-name="{{or propertyDetails.address propertyDetails.kaek}}">
                            <i class="ti-trash me-1"></i>
                            Διαγραφή Ακινήτου
                        </button>
                        
                        {{/if}}
                        
                        <a href="/properties/properties" class="btn btn-secondary">
                            <i class="ti-arrow-left me-1"></i>
                            Επιστροφή στη Λίστα Ακινήτων
                        </a>
                    </div>
                </div>
            </div>
            
            {{#if propertyDetails}}
            <!-- Πληροφορίες δραστηριότητας -->
            <div class="card mt-3">
                <div class="card-header">
                    <h6 class="card-title mb-0">
                        <i class="ti-info me-2"></i>
                        Πληροφορίες Ακινήτου
                    </h6>
                </div>
                <div class="card-body">
                    <small class="text-muted">
                        <p><strong>Δημιουργία:</strong><br>
                        {{date propertyDetails.createdAt}}</p>
                        
                        <p><strong>Τελευταία ενημέρωση:</strong><br>
                        {{date propertyDetails.updatedAt}}</p>
                        
                    </small>
                </div>
            </div>
            {{/if}}
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Form για επεξεργασία/δημιουργία property
    document.getElementById('propertyForm').addEventListener('submit', async function(e) {
        e.preventDefault();
        
        Q('bootstrap-spinner').show();
        const formData = new FormData(this);
        const data = Object.fromEntries(formData);
        const propertyId = data.id;
        const isEdit = !!propertyId; // true αν υπάρχει ID (επεξεργασία)
        
        if (isEdit) {
            delete data.id; // Αφαιρούμε το ID από τα data για PUT request
        }
        
        // Μετατροπή κενών strings σε null για numbers και dates
        if (data.area === '') data.area = null;
        if (data.construction_year === '') data.construction_year = null;
        
        // Μετατροπή string boolean σε boolean
        data.active = data.active === 'true';
        data.is_part_of_other = data.is_part_of_other === 'true';
        
        try {
            const url = isEdit ? `/properties/properties/${propertyId}` : '/properties/properties';
            const method = isEdit ? 'PUT' : 'POST';
            
            const response = await fetch(url, {
                method: method,
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(data)
            });
            
            const result = await response.json();
            Q('bootstrap-spinner').show(false);
            await Q.delay(0.1);
            
            if (result.success) {
                const message = isEdit ? 'Το Ακίνητο ενημερώθηκε επιτυχώς!' : 'Το Ακίνητο δημιουργήθηκε επιτυχώς!';

                // Εμφάνιση success modal με callback για navigation
                showSuccessModal(message, 3000, () => {
                    if (isEdit) {   // if updating
                        redirectOnSuccess(null); // refresh current page
                    } else {    // if creating new
                        redirectOnSuccess(`/properties/properties/${result.property.id}`);
                    }
                });
                
            } else {
                showErrorModal(result.message || 'Προέκυψε σφάλμα κατά την επεξεργασία της αίτησής σας.');
            }
        } catch (error) {
            Q('bootstrap-spinner').show(false);
            showErrorModal('Σφάλμα δικτύου: ' + error.message);
        }
    });
    
    // Button για διαγραφή property (μόνο σε επεξεργασία)
    document.querySelectorAll('.delete-property-btn').forEach(button => {
        button.addEventListener('click', async function() {
            const propertyId = this.dataset.propertyId;
            const propertyName = this.dataset.propertyName;

            if (confirm(`Είστε σίγουροι ότι θέλετε να διαγράψετε το Ακίνητο "${propertyName}";`)) {

                Q('bootstrap-spinner').show();
                try {
                    const response = await fetch(`/properties/properties/${propertyId}`, {
                        method: 'DELETE'
                    });
                    
                    const result = await response.json();
                    Q('bootstrap-spinner').show(false);

                    if (result.success) {
                        alert('Το Ακίνητο διαγράφηκε επιτυχώς!');
                        window.location.href = '/properties/properties';
                    } else {
                        alert(result.message);
                    }
                } catch (error) {
                    Q('bootstrap-spinner').show(false);
                    showErrorModal('Σφάλμα δικτύου: ' + error.message);
                }
            }
        });
    });
    
    // Κουμπί αντιγραφής link αρχείων
    document.getElementById('copyFileLinkBtn').addEventListener('click', async function() {
        const fileLinkInput = document.getElementById('fileServerLink');
        const fileLink = fileLinkInput.value;
        
        try {
            await navigator.clipboard.writeText(fileLink);
            
            // Αλλαγή κειμένου κουμπιού για feedback
            const originalHTML = this.innerHTML;
            this.innerHTML = '<i class="ti-check me-1"></i>Αντιγράφηκε!';
            this.classList.add('btn-success');
            this.classList.remove('btn-secondary');
            
            // Επαναφορά μετά από 2 δευτερόλεπτα
            setTimeout(() => {
                this.innerHTML = originalHTML;
                this.classList.remove('btn-success');
                this.classList.add('btn-secondary');
            }, 2000);
            
        } catch (error) {
            console.log('Αποτυχία αντιγραφής:', error);
        }
    });
});
</script>
